#pragma once
// Stub for host-side (native) unit test compilation.
// IRremoteESP8266 base types — nothing from this header is used directly
// in ir_lg.cpp; it is included only as a transitive dependency of ir_LG.h.
#include <cstdint>
